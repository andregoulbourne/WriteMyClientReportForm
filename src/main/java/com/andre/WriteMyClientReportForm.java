package com.andre;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * WriteMyClientReportForm is the main application class for the Write My Client Report Form application.
 * It initializes the Spring Boot application.
 * This application is designed to generate client reports based on user input and predefined templates.
 */
@SpringBootApplication
public class WriteMyClientReportForm {
	
	public static void main(String[] args) {
		SpringApplication.run(WriteMyClientReportForm.class, args);
	}
	
}
